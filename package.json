{
  "name": "shinobu-server",
  "version": "0.1.0",
  "private": true,
  "description": "Node.js manga translation service for pixiv-viewer (ONNX detection/OCR/inpaint + LLM translation + canvas typesetting)",
  "license": "AGPL-3.0-only",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "canvas": "^3.2.3",
    "express": "^5.2.1",
    "onnxruntime-node": "^1.27.0"
  }
}
